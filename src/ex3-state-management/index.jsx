import React from 'react';

// eslint-disable-next-line import/prefer-default-export
export const StateManagementExercise = (props) => {
  const d = props;
  return (
    <div>
      <input type="text" />
      {d}
      <ul>...</ul>
    </div>
  );
};
